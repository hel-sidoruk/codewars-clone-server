const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const highestAndLowest = functionsToTest['554b4ac871d6813a03000035'];

describe('', () => {
  it('', () => {
    assert.strictEqual(highestAndLowest(), '');
  });
  it('', () => {
    assert.strictEqual(highestAndLowest(), '');
  });
  it('', () => {
    assert.strictEqual(highestAndLowest(), '');
  });
  it('', () => {
    assert.strictEqual(highestAndLowest(), '');
  });
  it('', () => {
    assert.strictEqual(highestAndLowest(), '');
  });
});
