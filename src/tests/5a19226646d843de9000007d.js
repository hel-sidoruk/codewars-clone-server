const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const howManyUniqueConsonants = functionsToTest['5a19226646d843de9000007d'];

describe('', () => {
  it('', () => {
    assert.strictEqual(howManyUniqueConsonants(), '');
  });
  it('', () => {
    assert.strictEqual(howManyUniqueConsonants(), '');
  });
  it('', () => {
    assert.strictEqual(howManyUniqueConsonants(), '');
  });
  it('', () => {
    assert.strictEqual(howManyUniqueConsonants(), '');
  });
  it('', () => {
    assert.strictEqual(howManyUniqueConsonants(), '');
  });
});
