const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const studentsFinalGrade = functionsToTest['5ad0d8356165e63c140014d4'];

describe('', () => {
  it('', () => {
    assert.strictEqual(studentsFinalGrade(), '');
  });
  it('', () => {
    assert.strictEqual(studentsFinalGrade(), '');
  });
  it('', () => {
    assert.strictEqual(studentsFinalGrade(), '');
  });
  it('', () => {
    assert.strictEqual(studentsFinalGrade(), '');
  });
  it('', () => {
    assert.strictEqual(studentsFinalGrade(), '');
  });
});
