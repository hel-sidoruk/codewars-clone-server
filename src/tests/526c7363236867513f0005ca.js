const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const leapYears = functionsToTest['526c7363236867513f0005ca'];

describe('', () => {
  it('', () => {
    assert.strictEqual(leapYears(), '');
  });
  it('', () => {
    assert.strictEqual(leapYears(), '');
  });
  it('', () => {
    assert.strictEqual(leapYears(), '');
  });
  it('', () => {
    assert.strictEqual(leapYears(), '');
  });
  it('', () => {
    assert.strictEqual(leapYears(), '');
  });
});
