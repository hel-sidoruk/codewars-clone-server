const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const typeOfSum = functionsToTest['5a2e9ae2b6cfd7692a0000ba'];

describe('', () => {
  it('', () => {
    assert.strictEqual(typeOfSum(), '');
  });
  it('', () => {
    assert.strictEqual(typeOfSum(), '');
  });
  it('', () => {
    assert.strictEqual(typeOfSum(), '');
  });
  it('', () => {
    assert.strictEqual(typeOfSum(), '');
  });
  it('', () => {
    assert.strictEqual(typeOfSum(), '');
  });
});
