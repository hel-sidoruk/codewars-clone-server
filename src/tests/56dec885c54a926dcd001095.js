const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const oppositeNumber = functionsToTest['56dec885c54a926dcd001095'];

describe('', () => {
  it('', () => {
    assert.strictEqual(oppositeNumber(), '');
  });
  it('', () => {
    assert.strictEqual(oppositeNumber(), '');
  });
  it('', () => {
    assert.strictEqual(oppositeNumber(), '');
  });
  it('', () => {
    assert.strictEqual(oppositeNumber(), '');
  });
  it('', () => {
    assert.strictEqual(oppositeNumber(), '');
  });
});
