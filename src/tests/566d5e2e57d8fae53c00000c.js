const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const bingoCard = functionsToTest['566d5e2e57d8fae53c00000c'];

describe('', () => {
  it('', () => {
    assert.strictEqual(bingoCard(), '');
  });
  it('', () => {
    assert.strictEqual(bingoCard(), '');
  });
  it('', () => {
    assert.strictEqual(bingoCard(), '');
  });
  it('', () => {
    assert.strictEqual(bingoCard(), '');
  });
  it('', () => {
    assert.strictEqual(bingoCard(), '');
  });
});
