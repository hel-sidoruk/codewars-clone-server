const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const countConsonants = functionsToTest['564e7fc20f0b53eb02000106'];

describe('', () => {
  it('', () => {
    assert.strictEqual(countConsonants(), '');
  });
  it('', () => {
    assert.strictEqual(countConsonants(), '');
  });
  it('', () => {
    assert.strictEqual(countConsonants(), '');
  });
  it('', () => {
    assert.strictEqual(countConsonants(), '');
  });
  it('', () => {
    assert.strictEqual(countConsonants(), '');
  });
});
