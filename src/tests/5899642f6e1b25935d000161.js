const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const mergeTwoSortedArraysIntoOne = functionsToTest['5899642f6e1b25935d000161'];

describe('', () => {
  it('', () => {
    assert.strictEqual(mergeTwoSortedArraysIntoOne(), '');
  });
  it('', () => {
    assert.strictEqual(mergeTwoSortedArraysIntoOne(), '');
  });
  it('', () => {
    assert.strictEqual(mergeTwoSortedArraysIntoOne(), '');
  });
  it('', () => {
    assert.strictEqual(mergeTwoSortedArraysIntoOne(), '');
  });
  it('', () => {
    assert.strictEqual(mergeTwoSortedArraysIntoOne(), '');
  });
});
