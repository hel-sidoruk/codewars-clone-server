const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const pairOfGloves = functionsToTest['58235a167a8cb37e1a0000db'];

describe('', () => {
  it('', () => {
    assert.strictEqual(pairOfGloves(), '');
  });
  it('', () => {
    assert.strictEqual(pairOfGloves(), '');
  });
  it('', () => {
    assert.strictEqual(pairOfGloves(), '');
  });
  it('', () => {
    assert.strictEqual(pairOfGloves(), '');
  });
  it('', () => {
    assert.strictEqual(pairOfGloves(), '');
  });
});
