const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const convertToBinary = functionsToTest['59fca81a5712f9fa4700159a'];

describe('', () => {
  it('', () => {
    assert.strictEqual(convertToBinary(), '');
  });
  it('', () => {
    assert.strictEqual(convertToBinary(), '');
  });
  it('', () => {
    assert.strictEqual(convertToBinary(), '');
  });
  it('', () => {
    assert.strictEqual(convertToBinary(), '');
  });
  it('', () => {
    assert.strictEqual(convertToBinary(), '');
  });
});
