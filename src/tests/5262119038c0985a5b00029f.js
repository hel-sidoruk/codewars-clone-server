const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const isNumberPrime = functionsToTest['5262119038c0985a5b00029f'];

describe('', () => {
  it('', () => {
    assert.strictEqual(isNumberPrime(), '');
  });
  it('', () => {
    assert.strictEqual(isNumberPrime(), '');
  });
  it('', () => {
    assert.strictEqual(isNumberPrime(), '');
  });
  it('', () => {
    assert.strictEqual(isNumberPrime(), '');
  });
  it('', () => {
    assert.strictEqual(isNumberPrime(), '');
  });
});
