const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const countStringsInObjects = functionsToTest['565b3542af398bfb50000003'];

describe('', () => {
  it('', () => {
    assert.strictEqual(countStringsInObjects(), '');
  });
  it('', () => {
    assert.strictEqual(countStringsInObjects(), '');
  });
  it('', () => {
    assert.strictEqual(countStringsInObjects(), '');
  });
  it('', () => {
    assert.strictEqual(countStringsInObjects(), '');
  });
  it('', () => {
    assert.strictEqual(countStringsInObjects(), '');
  });
});
