const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const transportationOnVacation = functionsToTest['568d0dd208ee69389d000016'];

describe('', () => {
  it('', () => {
    assert.strictEqual(transportationOnVacation(), '');
  });
  it('', () => {
    assert.strictEqual(transportationOnVacation(), '');
  });
  it('', () => {
    assert.strictEqual(transportationOnVacation(), '');
  });
  it('', () => {
    assert.strictEqual(transportationOnVacation(), '');
  });
  it('', () => {
    assert.strictEqual(transportationOnVacation(), '');
  });
});
