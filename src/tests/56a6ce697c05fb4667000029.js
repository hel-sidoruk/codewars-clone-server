const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const nextPalindromicNumber = functionsToTest['56a6ce697c05fb4667000029'];

describe('', () => {
  it('', () => {
    assert.strictEqual(nextPalindromicNumber(), '');
  });
  it('', () => {
    assert.strictEqual(nextPalindromicNumber(), '');
  });
  it('', () => {
    assert.strictEqual(nextPalindromicNumber(), '');
  });
  it('', () => {
    assert.strictEqual(nextPalindromicNumber(), '');
  });
  it('', () => {
    assert.strictEqual(nextPalindromicNumber(), '');
  });
});
