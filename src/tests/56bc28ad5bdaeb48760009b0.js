const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const removeFirstAndLastCharacter = functionsToTest['56bc28ad5bdaeb48760009b0'];

describe('', () => {
  it('', () => {
    assert.strictEqual(removeFirstAndLastCharacter(), '');
  });
  it('', () => {
    assert.strictEqual(removeFirstAndLastCharacter(), '');
  });
  it('', () => {
    assert.strictEqual(removeFirstAndLastCharacter(), '');
  });
  it('', () => {
    assert.strictEqual(removeFirstAndLastCharacter(), '');
  });
  it('', () => {
    assert.strictEqual(removeFirstAndLastCharacter(), '');
  });
});
