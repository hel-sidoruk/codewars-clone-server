const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const countTheDigit = functionsToTest['566fc12495810954b1000030'];

describe('', () => {
  it('', () => {
    assert.strictEqual(countTheDigit(), '');
  });
  it('', () => {
    assert.strictEqual(countTheDigit(), '');
  });
  it('', () => {
    assert.strictEqual(countTheDigit(), '');
  });
  it('', () => {
    assert.strictEqual(countTheDigit(), '');
  });
  it('', () => {
    assert.strictEqual(countTheDigit(), '');
  });
});
