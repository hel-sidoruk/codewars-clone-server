const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const perpendicularLines = functionsToTest['6391fe3f322221003db3bad6'];

describe('', () => {
  it('', () => {
    assert.strictEqual(perpendicularLines(), '');
  });
  it('', () => {
    assert.strictEqual(perpendicularLines(), '');
  });
  it('', () => {
    assert.strictEqual(perpendicularLines(), '');
  });
  it('', () => {
    assert.strictEqual(perpendicularLines(), '');
  });
  it('', () => {
    assert.strictEqual(perpendicularLines(), '');
  });
});
