const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const unluckyDays = functionsToTest['56eb0be52caf798c630013c0'];

describe('', () => {
  it('', () => {
    assert.strictEqual(unluckyDays(), '');
  });
  it('', () => {
    assert.strictEqual(unluckyDays(), '');
  });
  it('', () => {
    assert.strictEqual(unluckyDays(), '');
  });
  it('', () => {
    assert.strictEqual(unluckyDays(), '');
  });
  it('', () => {
    assert.strictEqual(unluckyDays(), '');
  });
});
