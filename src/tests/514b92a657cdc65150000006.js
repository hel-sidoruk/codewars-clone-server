const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const multiplesOf3Or5 = functionsToTest['514b92a657cdc65150000006'];

describe('', () => {
  it('', () => {
    assert.strictEqual(multiplesOf3Or5(), '');
  });
  it('', () => {
    assert.strictEqual(multiplesOf3Or5(), '');
  });
  it('', () => {
    assert.strictEqual(multiplesOf3Or5(), '');
  });
  it('', () => {
    assert.strictEqual(multiplesOf3Or5(), '');
  });
  it('', () => {
    assert.strictEqual(multiplesOf3Or5(), '');
  });
});
