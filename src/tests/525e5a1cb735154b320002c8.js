const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const triangularTreasure = functionsToTest['525e5a1cb735154b320002c8'];

describe('', () => {
  it('', () => {
    assert.strictEqual(triangularTreasure(), '');
  });
  it('', () => {
    assert.strictEqual(triangularTreasure(), '');
  });
  it('', () => {
    assert.strictEqual(triangularTreasure(), '');
  });
  it('', () => {
    assert.strictEqual(triangularTreasure(), '');
  });
  it('', () => {
    assert.strictEqual(triangularTreasure(), '');
  });
});
