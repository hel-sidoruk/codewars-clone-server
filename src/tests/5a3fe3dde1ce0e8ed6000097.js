const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const centuryFromYear = functionsToTest['5a3fe3dde1ce0e8ed6000097'];

describe('', () => {
  it('', () => {
    assert.strictEqual(centuryFromYear(), '');
  });
  it('', () => {
    assert.strictEqual(centuryFromYear(), '');
  });
  it('', () => {
    assert.strictEqual(centuryFromYear(), '');
  });
  it('', () => {
    assert.strictEqual(centuryFromYear(), '');
  });
  it('', () => {
    assert.strictEqual(centuryFromYear(), '');
  });
});
