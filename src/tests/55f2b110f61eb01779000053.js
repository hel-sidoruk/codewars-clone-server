const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const beginnerSeries3SumOfNumbers = functionsToTest['55f2b110f61eb01779000053'];

describe('', () => {
  it('', () => {
    assert.strictEqual(beginnerSeries3SumOfNumbers(), '');
  });
  it('', () => {
    assert.strictEqual(beginnerSeries3SumOfNumbers(), '');
  });
  it('', () => {
    assert.strictEqual(beginnerSeries3SumOfNumbers(), '');
  });
  it('', () => {
    assert.strictEqual(beginnerSeries3SumOfNumbers(), '');
  });
  it('', () => {
    assert.strictEqual(beginnerSeries3SumOfNumbers(), '');
  });
});
