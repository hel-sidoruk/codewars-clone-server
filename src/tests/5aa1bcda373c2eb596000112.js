const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const maximumTripletSum = functionsToTest['5aa1bcda373c2eb596000112'];

describe('', () => {
  it('', () => {
    assert.strictEqual(maximumTripletSum(), '');
  });
  it('', () => {
    assert.strictEqual(maximumTripletSum(), '');
  });
  it('', () => {
    assert.strictEqual(maximumTripletSum(), '');
  });
  it('', () => {
    assert.strictEqual(maximumTripletSum(), '');
  });
  it('', () => {
    assert.strictEqual(maximumTripletSum(), '');
  });
});
