const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const removeStringSpaces = functionsToTest['57eae20f5500ad98e50002c5'];

describe('', () => {
  it('', () => {
    assert.strictEqual(removeStringSpaces(), '');
  });
  it('', () => {
    assert.strictEqual(removeStringSpaces(), '');
  });
  it('', () => {
    assert.strictEqual(removeStringSpaces(), '');
  });
  it('', () => {
    assert.strictEqual(removeStringSpaces(), '');
  });
  it('', () => {
    assert.strictEqual(removeStringSpaces(), '');
  });
});
