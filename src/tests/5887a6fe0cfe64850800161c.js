const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const largestSquareInsideACircle = functionsToTest['5887a6fe0cfe64850800161c'];

describe('', () => {
  it('', () => {
    assert.strictEqual(largestSquareInsideACircle(), '');
  });
  it('', () => {
    assert.strictEqual(largestSquareInsideACircle(), '');
  });
  it('', () => {
    assert.strictEqual(largestSquareInsideACircle(), '');
  });
  it('', () => {
    assert.strictEqual(largestSquareInsideACircle(), '');
  });
  it('', () => {
    assert.strictEqual(largestSquareInsideACircle(), '');
  });
});
