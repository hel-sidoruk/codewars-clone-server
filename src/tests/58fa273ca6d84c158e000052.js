const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const numberOfDecimalDigits = functionsToTest['58fa273ca6d84c158e000052'];

describe('', () => {
  it('', () => {
    assert.strictEqual(numberOfDecimalDigits(), '');
  });
  it('', () => {
    assert.strictEqual(numberOfDecimalDigits(), '');
  });
  it('', () => {
    assert.strictEqual(numberOfDecimalDigits(), '');
  });
  it('', () => {
    assert.strictEqual(numberOfDecimalDigits(), '');
  });
  it('', () => {
    assert.strictEqual(numberOfDecimalDigits(), '');
  });
});
