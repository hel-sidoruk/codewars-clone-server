const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const arrayReduce = functionsToTest['5411e3e95f3a7f6a7a0000e3'];

describe('', () => {
  it('', () => {
    assert.strictEqual(arrayReduce(), '');
  });
  it('', () => {
    assert.strictEqual(arrayReduce(), '');
  });
  it('', () => {
    assert.strictEqual(arrayReduce(), '');
  });
  it('', () => {
    assert.strictEqual(arrayReduce(), '');
  });
  it('', () => {
    assert.strictEqual(arrayReduce(), '');
  });
});
