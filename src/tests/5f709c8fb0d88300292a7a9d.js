const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const whoIsTheKiller = functionsToTest['5f709c8fb0d88300292a7a9d'];

describe('', () => {
  it('', () => {
    assert.strictEqual(whoIsTheKiller(), '');
  });
  it('', () => {
    assert.strictEqual(whoIsTheKiller(), '');
  });
  it('', () => {
    assert.strictEqual(whoIsTheKiller(), '');
  });
  it('', () => {
    assert.strictEqual(whoIsTheKiller(), '');
  });
  it('', () => {
    assert.strictEqual(whoIsTheKiller(), '');
  });
});
