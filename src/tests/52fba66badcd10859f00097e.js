const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const disemvowelTrolls = functionsToTest['52fba66badcd10859f00097e'];

describe('', () => {
  it('', () => {
    assert.strictEqual(disemvowelTrolls(), '');
  });
  it('', () => {
    assert.strictEqual(disemvowelTrolls(), '');
  });
  it('', () => {
    assert.strictEqual(disemvowelTrolls(), '');
  });
  it('', () => {
    assert.strictEqual(disemvowelTrolls(), '');
  });
  it('', () => {
    assert.strictEqual(disemvowelTrolls(), '');
  });
});
