const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const whichColorIsTheBrightest = functionsToTest['62eb800ba29959001c07dfee'];

describe('', () => {
  it('', () => {
    assert.strictEqual(whichColorIsTheBrightest(), '');
  });
  it('', () => {
    assert.strictEqual(whichColorIsTheBrightest(), '');
  });
  it('', () => {
    assert.strictEqual(whichColorIsTheBrightest(), '');
  });
  it('', () => {
    assert.strictEqual(whichColorIsTheBrightest(), '');
  });
  it('', () => {
    assert.strictEqual(whichColorIsTheBrightest(), '');
  });
});
