const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const reversedWords = functionsToTest['51c8991dee245d7ddf00000e'];

describe('', () => {
  it('', () => {
    assert.strictEqual(reversedWords(), '');
  });
  it('', () => {
    assert.strictEqual(reversedWords(), '');
  });
  it('', () => {
    assert.strictEqual(reversedWords(), '');
  });
  it('', () => {
    assert.strictEqual(reversedWords(), '');
  });
  it('', () => {
    assert.strictEqual(reversedWords(), '');
  });
});
