const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const spoonerizeMe = functionsToTest['56b8903933dbe5831e000c76'];

describe('', () => {
  it('', () => {
    assert.strictEqual(spoonerizeMe(), '');
  });
  it('', () => {
    assert.strictEqual(spoonerizeMe(), '');
  });
  it('', () => {
    assert.strictEqual(spoonerizeMe(), '');
  });
  it('', () => {
    assert.strictEqual(spoonerizeMe(), '');
  });
  it('', () => {
    assert.strictEqual(spoonerizeMe(), '');
  });
});
