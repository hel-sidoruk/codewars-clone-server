const { assert } = require('chai');
const { describe, it } = require('mocha');
const functionsToTest = require('./functionsToTest');

const convertStringToNumber = functionsToTest['544675c6f971f7399a000e79'];

describe('', () => {
  it('', () => {
    assert.strictEqual(convertStringToNumber(), '');
  });
  it('', () => {
    assert.strictEqual(convertStringToNumber(), '');
  });
  it('', () => {
    assert.strictEqual(convertStringToNumber(), '');
  });
  it('', () => {
    assert.strictEqual(convertStringToNumber(), '');
  });
  it('', () => {
    assert.strictEqual(convertStringToNumber(), '');
  });
});
